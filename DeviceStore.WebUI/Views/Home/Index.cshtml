@using DeviceStore.Domain.Entities
@model IEnumerable<Device>

@{ 
    ViewBag.Title = "DeviceStore";
}

@foreach(Device d in @Model)
{
    <div>
        <h3>@d.DeviceName</h3>
        <p>@d.DeviceDescription</p>
        <h4>@d.DevicePrice.ToString("# руб.")</h4>
    </div>
}