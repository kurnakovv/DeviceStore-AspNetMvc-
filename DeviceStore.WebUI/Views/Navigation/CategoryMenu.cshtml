
@model IEnumerable<string>

@foreach(var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Home",
    action = "Index",
    categoryDevices = link,
},
    new
    {
        @class = "btn btn-block btn-warning",
    });
}